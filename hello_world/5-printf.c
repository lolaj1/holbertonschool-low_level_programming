#include <stdio.h>

/**
 * main - entry point of the program
 *
 * printf - print the line needed
 *
 * Return: upon no errors, returns a 0.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
